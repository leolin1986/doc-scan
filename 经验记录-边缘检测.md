# 文档扫描边缘检测 — 开发经验记录

> 日期：基于最近一次对话

## 核心逻辑

1. **Canny 边缘检测** → 生成边缘图
2. **Hough 直线变换** → 提取线段
3. **聚类分组** → 水平线/垂直线各聚成簇
4. **枚举四边形候选** → 从线对组合中评分筛选
5. **透视校正** → 把检测到的文档拉正

## 关键参数 & 经验

### 排序策略
- **hPairs / vPairs 按票数和降序**：边越清晰（像素越多）越优先处理
- 候选上限：hPairs ≤ 20, vPairs ≤ 24（防性能爆炸，同时覆盖足够候选）

### 筛选条件
- 线对的最小间距 `minDist = min(w, h) * 0.1`（太近的平行线不是文档对边）
- 角点可超出图像边界 `margin = max(w, h) * 0.5`（文档边缘可能在图像外）
- 最终分数阈值 `bestScore < 0.15` 视为无检测

### 评分公式（最高分胜出）
```
angleScore × 0.25   — 四角接近 90° 的程度
areaScore   × 0.30   — 四边形面积占图比（越大越好，文档通常大）
convex      × 0.10   — 凸性（凹四边形得 0.3）
aspectScore × 0.10   — 边长比（避免太扁长）
insideCount × 0.10   — 4顶点在图像内的数量
boundsScore × 0.10   — 越界惩罚（超出图像边界扣分）
```

### 聚类参数
- 多组尝试：`[0.8, 1.0, 1.4]` —— 不同文档边界清晰度适用不同的聚类敏感度

## ⚠️ 踩坑记录

### ❌ 1. 边缘惩罚（Edge Penalty） — 失败
**尝试**：惩罚顶点靠近图像边界的四边形，想阻止"全图四边形"（四个顶点正好顶到图四角）。

**结果**：合法文档边缘也可能靠近图像边界，被误伤 → 只能检测到文档右半边，左上角被切掉。

**教训**：不要惩罚"靠近图像边界的顶点"—— 合法文档边缘本身就靠近图像边界。应该惩罚的是**顶点挤在图像角落**（全图四边形的特征）。

### ❌ 2. 角点惩罚（Corner Penalty） — 失败
**尝试**：改为只惩罚顶点靠近**图像角落**（不是边缘），用顶点到4个图角的欧几里得距离。

**结果**：依然误伤。如果文档几乎填满画面，左上角文档角点 = 图像左上角 (0,0)，被当作"退化"。

**教训**：全图四边形问题不能靠惩罚"靠近图角的顶点"解决——对真正填满画面的文档不公平。

### ✅ 未测试的替代思路
如果以后想解决"全图四边形"问题，可以考虑：
- **面积比上限**：areaScore 在面积占比 > 0.85 时逐步衰减（排除全图四边形，不同时排除真正的满屏文档）
- **检查顶点的"共线于图边"倾向**：如果 4 个顶点正好都落在图像边界上（x=0/x=w/y=0/y=h），则退化

## 构建相关
- TypeScript 编译：`npx tsc --noEmit`
- 端口冲突时：`taskkill /f /pid PID` 或换端口 `npm run dev -- -p 3001`
- `.next` 缓存问题：删除 `.next` 目录重建（`rmdir /s /q .next`）
